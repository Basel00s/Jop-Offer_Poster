# MongoDB connection string (same database as the main app)
MONGO_URI=mongodb://mongo:27017/job-poster

# Delay range between jobs in seconds (min-max)
MIN_DELAY=45
MAX_DELAY=180

# Cooldown duration in hours when rate-limited
COOLDOWN_HOURS=4
